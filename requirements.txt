# Curated runtime dependencies for the Flexi_CT package, notebooks, demo gallery,
# tests, and checked-in downstream workflows.
#
# Pins match the working `flexict` conda env exported in environment_full.yml
# and requirements_full.txt on this host (Python 3.11.15). The full exports are
# archival snapshots; this file keeps direct project dependencies only.
# Transitive deps such as tokenizers, safetensors, huggingface-hub, and
# nvidia-cu12-* resolve automatically from these pins.

# Core compute
torch==2.8.0          # ships with CUDA 12.8 wheels by default from PyPI
torchvision==0.23.0   # matches torch 2.8.0; used by downstream/vlm/dataset.py
numpy==2.3.2

# VLM text tower (HuggingFace AutoModel/AutoTokenizer for Qwen3-Embedding-0.6B)
transformers==4.56.1
datasets==4.4.1
pandas==2.3.2
scikit-learn==1.7.1
scipy==1.16.1
scikit-image==0.25.2
monai==1.5.1
einops==0.8.1
opencv-python==4.11.0.86
pyyaml==6.0.2
tqdm==4.67.1
requests==2.32.5
lmdb==1.7.3

# Notebook, demo, and medical-image I/O
matplotlib==3.10.5
SimpleITK==2.5.2      # .nii / .nii.gz reading
nibabel==5.3.2        # registration NIfTI I/O
pillow==11.3.0        # .png / .jpg reading

# Notebook runtime
jupyterlab==4.4.6
ipykernel==6.30.1
ipython==9.4.0
ipywidgets==8.1.7

# Downstream segmentation (local nnU-Net workflow under downstream/segmentation)
batchgenerators==0.25.1
argparse==1.4.0     # batchgenerators -> unittest2 metadata dependency in the exported env
batchgeneratorsv2==0.3.0
acvl-utils==0.2.5
dynamic-network-architectures==0.4.2
graphviz==0.21
tifffile==2025.8.28
seaborn==0.13.2
imagecodecs==2025.8.2
yacs==0.1.8
blosc2==3.10.2
surface-distance==0.1
threadpoolctl==3.6.0
